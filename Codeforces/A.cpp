#include <bits/stdc++.h>
using namespace std;

void solve() {
    int n, m;
    cin >> n >> m;

    if (n == 1 || m == 1) {
        cout << "1 1\n";
        return;
    }

    cout << "2 2\n";
}

int main() {
    int t;
    cin >> t;
    while (t-- > 0) {
        solve();
    }
}